server:
    port: 8080

spring:

    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true

    config:
        location:
            - classpath: /application-oAuth.yml